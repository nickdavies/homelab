---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: claude-notify-work
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: claude-notify-work-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: claude-notify-work
