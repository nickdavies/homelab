apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: k8s-gateway
spec:
  interval: 5m
  # Upstream moved to Codeberg; the GitHub Pages index now 404s.
  url: https://k8s-gateway.codeberg.page/k8s_gateway/
